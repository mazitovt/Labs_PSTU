@model IEnumerable<MilitaryDistrictAPI.Models.scaffolded.SoldierOccupation>

@{
    ViewData["Title"] = "Специальности военнослужащих";
    Layout = "CRUD/_LayoutIndex";
}

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Soldier.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Occupation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Soldier.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Occupation.Name)
            </td>
            <td>
                @Html.ActionLink("Изменить", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Подробнее", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Удалить", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>

