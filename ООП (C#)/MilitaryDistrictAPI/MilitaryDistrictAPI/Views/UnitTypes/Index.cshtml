@model IEnumerable<MilitaryDistrictAPI.Models.scaffolded.UnitType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutCRUD.cshtml";
}

<h1>Типы войсковых частей</h1>

<p>
    @*<a asp-action="Create">Create New</a>*@
</p>
<table class="table col-2">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UnitTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UnitTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.UnitTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnitTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnitTypeId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
