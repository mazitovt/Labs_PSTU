@model IEnumerable<MilitaryDistrictAPI.Models.scaffolded.SubunitType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutCRUD.cshtml";
}

<h1>Типы подразделений</h1>

<p>
    @*<a asp-action="Create">Create New</a>*@
</p>
<table class="table col-2">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubunitTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubunitTypeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        @*<td>
                <a asp-action="Edit" asp-route-id="@item.SubunitTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SubunitTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SubunitTypeId">Delete</a>
            </td>*@
    </tr>
}
    </tbody>
</table>
