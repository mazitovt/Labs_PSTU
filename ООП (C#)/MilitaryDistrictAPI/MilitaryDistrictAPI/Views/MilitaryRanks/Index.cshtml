@model IEnumerable<MilitaryDistrictAPI.Models.scaffolded.MilitaryRank>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutCRUD.cshtml";
}

<h1>Звания</h1>

<p>
    @*<a asp-action="Create">Create New</a>*@
</p>
<table class="table col-8">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MilitaryRankId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RankGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommandHigherUnit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MilitaryRankId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RankGroup)
            </td>
            <td>
                @*<input class="form-check-input" type="checkbox" value="" id="flexCheckChecked" checked>*@
                @*@Html.DisplayFor(modelItem => item.CommandHigherUnit)*@
                @{ 
                    string ch = "";
                    if (item.CommandHigherUnit)
                    {
                        ch = "checked=\"\"";
                    }
                }
                <div class="custom-control custom-checkbox">
                    <input type="checkbox" class="custom-control-input" id="defaultCheckedDisabled" @ch disabled="">
                    <label class="custom-control-label" for="defaultCheckedDisabled"></label>
                </div>
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.MilitaryRankId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MilitaryRankId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MilitaryRankId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
